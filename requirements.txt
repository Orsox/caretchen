PySide6>=6.7
pynput>=1.7
sounddevice>=0.4
soundfile>=0.12
numpy>=1.26
faster-whisper>=1.0
httpx>=0.27
pyperclip>=1.9
tomli-w>=1.0
PyYAML>=6.0
evdev>=1.9; sys_platform == 'linux'
# Wayland Portal-Maus-Hook (optional, benötigt Python <= 3.13):
# pip install pyinputcapture
# Wayland Paste-Tools (system packages):
#   ydotool  — Sway / Hyprland / Wayfire
#   wtype    — GNOME
#   xdotool  — XWayland
